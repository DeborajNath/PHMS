import 'package:flutter/widgets.dart';

class ReturnCaseList extends StatelessWidget {
  const ReturnCaseList({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('return List');
  }
}
