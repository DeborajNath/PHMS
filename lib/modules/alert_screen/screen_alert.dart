import 'package:flutter/material.dart';

class ScreenAlert extends StatelessWidget {
  const ScreenAlert({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Screen Alert ?? Ui not provided'));
  }
}
